#include <iostream>
#include <vector>
using namespace std;

bool uniqueOccurrences(vector<int>& arr) {
        //1 2 2 1 1 3
    vector<int> a;
    int count = 0;
    for (int i=0; i<arr.size(); i++)
    {
        
    }
}

int main()
{
    int n;
    cout << "Enter size of vector : ";
    cin >> n;

    vector<char>s;

    for (int i=0; i<n; i++)
    {
        char x;
        cin>>x;
        s.push_back(x);
    }


    return 0;
}